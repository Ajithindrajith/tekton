replicaCount: 1

image:
  repository: antdemoacr.azurecr.io/front-ui
  tag: latest
  pullPolicy: Always

service:
  type: LoadBalancer   # 🔥 change this
  port: 80
  targetPort: 80

cosmos:
  secretName: cosmos-secret
  key: COSMOS_KEY

keyVault:
  secretProviderClass: cosmos-kv-secret